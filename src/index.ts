// @ts-ignore
const app = document.getElementById("app");
if (app) {
  app.innerHTML = `
  <h1>Utility Types</h1>
  <p>This practice will have you re-create the Utility Types which ship with TypeScript. You can find the templates and examples in the <code>src/utilityTypes.ts</code> file.
  `;
}
